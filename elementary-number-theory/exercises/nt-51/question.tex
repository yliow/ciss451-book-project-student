\tinysidebar{\debug{exercises/{nt-51/question.tex}}}
  Prove that if $a \neq 0$, then
  there are many possible $x, y$ such that
  $ax + by = \gcd(a,b)$.
  \qed

% ax + by = ax + by + ab - ab = a(x + b) + b(y - a).
% ax + by = ax + by + nab - nab = a(x + nb) + b(y - na).

\tinysidebar{\debug{exercises/{vigenere-4/question.tex}}}
  Using the above, write a function that
  computes a probabilistic guess on the
  length of the key when given a string encrypted using
  the Vigenere cipher.
